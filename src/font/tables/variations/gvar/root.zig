//! gvar structural and glyf-target validation.

const validation = @import("validation.zig");

pub const TargetContext = validation.TargetContext;
pub const targetCount = validation.targetCount;
pub const validate = validation.validate;
