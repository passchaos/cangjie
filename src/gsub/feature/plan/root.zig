//! Staged GSUB lookup-plan selection and ownership.

pub const apply = @import("apply/root.zig");
pub const build = @import("build.zig");
pub const merge = @import("merge.zig");
pub const selection = @import("selection.zig");
