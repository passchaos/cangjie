//! GSUB structural and glyph-bound validation domains.

pub const coverage_array = @import("coverage_array.zig");
pub const direct = @import("direct/root.zig");
pub const reverse = @import("reverse.zig");
