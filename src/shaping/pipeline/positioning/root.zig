//! Final positioning infrastructure shared by GPOS, AAT, and output emission.

pub const adjustments = @import("adjustments.zig");
pub const attachments = @import("attachments.zig");
pub const collect = @import("collect.zig");
pub const engine = @import("engine.zig");
pub const output = @import("output.zig");
pub const policy = @import("policy.zig");
pub const source_span = @import("source_span.zig");
